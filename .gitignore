# 依赖
node_modules/

# 构建/打包产物（保留在磁盘，不入版本库）
dist/
releases/

# 旧构建/打包临时残留
dist-bak-*/
tmp_*/
_harden_src/
_harden_out/

# asar 备份
*.bak
*.bak2

# 运行时用户数据
data/

# 项目记忆（AI 工作记忆，非源码）
.workbuddy/

# 系统/编辑器
.DS_Store
Thumbs.db
*.log

# 激活服务器私钥（绝不可提交，否则任何人可伪造激活码）
license-server/keys/private.pem
license-server/revoked.json
license-server/revoked-tokens.json
license-server/orders.json
license-server/teams.json
license-server/pay-config.json

# 沙箱专用测试脚本（含本机绝对 node 路径，不入库）
_test_*.mjs
