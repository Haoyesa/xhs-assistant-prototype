# 合规核查清单 · 黑猫智记AI 重新定位

> 目标：确保产品符合「通用内容创作辅助工具」定位——
> 不接入平台接口、不自动发布、不批量管账号、不爬取平台数据、不收集账号密码、品牌与代码合规。

## A. 代码与开源协议合规

- [ ] **依赖许可证核查**
  - `electron` (MIT) ✅ 合规
  - `electron-builder` (MIT) ✅ 合规
  - `puppeteer-core` (MIT) ⚠️ 仅被 `cdp-publisher.js` 使用 → 阶段 2 删除发布代码后，**从 package.json 移除该依赖**
  - `license-server/` 零外部依赖 ✅
- [ ] **无第三方代码侵权**：确认仓库无 `resources/`、`vendor/` 等捆绑第三方源码；`dist/` 仅含 Electron/Chromium 自带二进制与许可文件（属构建产物）。
- [ ] **LICENSE 字段**：根 `package.json` 与 `license-server/package.json` 均缺少 `"license"` 字段 → 补充（如 `"license": "MIT"` 或明确「自用/未开源」声明）。
- [ ] **逆向分析痕迹清理**：`ARCHITECTURE.md` 引用了 `软件拆解与可行性分析.md`（对原商业软件的逆向分析）。确认仓库内无该逆向产物；移除文档中的逆向引用表述，避免被认定为规避技术措施。

## B. 商标与品牌合规

- [ ] **移除平台商标/品牌词**：全仓扫描并替换 `小红书 / xhs / XHS / 薯 / 笔记 / 种草 / 千帆` 等平台专属表述（排查命中：content-creator.js 200 处、panel.css 23 处、generator.html 7 处、app.js 7 处、server.js 23 处、common.js/background.js 中 `[XHS]` 日志、popup.html、README/ARCHITECTURE 等）。
- [ ] **不使用平台视觉标识**：确认 UI 无小红书 logo、品牌色块、专属图标；封面/排版参考中的示例素材不使用任何平台受版权保护的素材。
- [ ] **自有品牌保留**：「黑猫智记AI」为自有品牌，可保留；对外卖点改为「本地内容创作辅助工具」。
- [ ] **appId 去平台关联**：`package.json` `appId: com.example.xhsassistant` → 改为 `com.heimao.aicontent`（去掉 `xhs`）。
- [ ] **存储键重命名**：`xhs-*` / `xh-` 前缀的 chrome.storage 键、CSS 类名改为平台中立前缀（如 `hm-*`）。

## C. 数据合规底线

- [ ] **不收集平台账号密码**：确认无账号密码输入框、无 Cookie/登录态抓取；`/api/accounts` 多账号模块删除。
- [ ] **数据不上传厂商服务器**：确认除「用户自行配置的 AI 服务商端点」外，无任何出站内容传输；机器码仅发往激活服务器（license-server），不含任何平台凭证。
- [ ] **不爬取平台数据**：删除 `qianfan-scraper.js`、`extension/content-ark.js` 千帆爬取；移除 `/api/qianfan/*`、`/api/ext/products` 等接收/抓取平台数据的接口。
- [ ] **本地存储**：编辑结果、素材、历史均存于用户本机 `data/`；确认无隐性上报。
- [ ] **图片处理收敛**：`image-util.js` 仅保留本地图片读取；移除 `downloadOne` 远程抓取（仅供爬取/发布使用）。

## D. 功能边界合规

- [ ] **无自动发布**：删除 `cdp-publisher.js`、extension 的 `realDebugClick`/`startPublish`、`server.js` 的 `runPump` 与 `/api/cdp/*`、`/api/batch/pump`。
- [ ] **无多账号批量管理**：删除 `/api/accounts` 及前端「账号」tab、账号配额门禁。
- [ ] **无平台接口接入**：移除所有 `creator.xiaohongshu.com` / `channel.xiaohongshu.com` 等平台 URL 与 `chrome.debugger` 权限。
- [ ] **敏感词检测落地**：新增本地敏感词模块，在用户协议中对应「禁止违规用途」条款形成产品内护栏。

## E. 发布前最终核对

- [ ] `npm run dist` 打包成功，启动后**默认无任何平台发布/采集/账号入口**。
- [ ] 全仓 `grep -ri "xiaohongshu|xhs|千帆|种草|creator.xiaohongshu"` 无业务残留（仅历史文档/注释可忽略，但建议一并清理）。
- [ ] 用户协议已内置并可在「设置/关于」查看。
- [ ] 在 git 打 `pre-reposition` tag 以便回滚阶段 2 的破坏性改动。
